apiVersion: v1
kind: Service
metadata:
  name: ecomm-product-service
  namespace: ecomm-staging
spec:
  selector:
    app: ecomm-product-service
  ports:
    - port: 3001
      targetPort: 3001
      nodePort: 30101
  type: NodePort
