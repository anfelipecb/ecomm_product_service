apiVersion: v1
kind: Service
metadata:
  name: ecomm-product-service
  namespace: ecomm-prod
spec:
  selector:
    app: ecomm-product-service
  ports:
    - port: 3001
      targetPort: 3001
      nodePort: 30201
  type: NodePort
